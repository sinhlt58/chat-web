import app from "./app.json";
import common from "./common.json";
import conversationsManagement from "./conversations-management.json";

const translation = {
  app,
  common,
  conversationsManagement,
};

export default translation;
